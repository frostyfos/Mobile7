import 'package:flutter/material.dart';

const kMainLabelTextStyle = TextStyle(
  fontSize: 20.0,
  color: Colors.black,
);

//Main Icon Button Constants -- Start
const kMainIconButtonLabelTextStyle = TextStyle(
  color: Colors.black54,
  fontWeight: FontWeight.w500,
);
//Main Icon Button Constants -- End

//Announcement Constants -- Start
const kAnnouncementContainerTopDecoration = BoxDecoration(
  borderRadius: BorderRadius.only(
    topLeft: Radius.circular(10.0),
    topRight: Radius.circular(10.0),
  ),
  color: Colors.white,
);

const kAnnouncementContainerBottomDecoration = BoxDecoration(
  borderRadius: BorderRadius.only(
    bottomLeft: Radius.circular(10.0),
    bottomRight: Radius.circular(10.0),
  ),
  color: Colors.white,
);

const kAnnouncementTitleStyle = TextStyle(
  fontSize: 20.0,
  fontWeight: FontWeight.bold,
);
//Announcement Constants -- End
